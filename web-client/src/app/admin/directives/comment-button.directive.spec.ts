import { CommentButtonDirective } from './comment-button.directive';

describe('CommentButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentButtonDirective();
    expect(directive).toBeTruthy();
  });
});
