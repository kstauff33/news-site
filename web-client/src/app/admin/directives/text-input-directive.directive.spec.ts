import { TextInputDirectiveDirective } from './text-input-directive.directive';

describe('TextInputDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TextInputDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
